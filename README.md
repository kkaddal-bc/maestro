# maestro

## Layout

```text
maestro/
  skills/
    maestro-snap/
      SKILL.md
      SCAN-PATTERNS.md
      OUTPUT-TEMPLATES.md
  packages/
  brew/
  .github/
    workflows/
```

The `skills/maestro-snap/` directory is the canonical repo-local copy of the first bundled skill.
The top-level scaffold includes `skills/`, `packages/`, `brew/`, and `.github/workflows/`.
