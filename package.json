{
  "name": "maestro",
  "private": true,
  "scripts": {
    "typecheck": "cd cli && CGO_ENABLED=0 $HOME/.local/bin/go test ./...",
    "test": "cd cli && CGO_ENABLED=0 $HOME/.local/bin/go test ./..."
  }
}
